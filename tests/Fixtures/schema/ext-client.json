{
    "id": "schema://entities/ext-client",
    "definitions": {
        "company": {
            "$ref": "schema://entities/derived/company"
        }
    },
    "type": "object",
    "properties": {
        "name": { "type": "string" },
        "phone": { "type": "string" },
        "company": { "$ref": "#/definitions/company" }
    },
    "required": ["name", "phone", "company"]
}